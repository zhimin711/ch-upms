spring:
  application:
    name: ch-upms
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.104:8848
        namespace: ${nacos.namespace:}
      config:
        server-addr: 192.168.1.104:8848
        namespace: ${nacos.namespace:}
#        shared-dataids: ch-upms.yml #,ch-sentinel.properties
        file-extension: yml
